# PDF text extraction
PyMuPDF>=1.24.0

# LangChain components (modern split packages)
langchain-core>=0.2
langchain-community>=0.2
langchain-text-splitters>=0.2

# Web framework and API
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
requests>=2.31.0
python-dotenv>=1.0

# Data processing and analysis
pandas>=2.0.0
nltk>=3.8
numpy>=1.26
scipy>=1.11
scikit-learn>=1.3
yfinance>=0.2.40
textblob>=0.19.0

# Optional PDF table extraction (better financial data parsing)
pdfplumber>=0.9.0

# Forecasting models
prophet>=1.1.5
cmdstanpy>=1.2
matplotlib>=3.8
# Use platform-appropriate TensorFlow
tensorflow-macos>=2.15; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow>=2.15; sys_platform != 'darwin' or platform_machine != 'arm64'

# Fallback single-package (optional). Uncomment if you prefer the monolith.
# langchain>=0.1.0
